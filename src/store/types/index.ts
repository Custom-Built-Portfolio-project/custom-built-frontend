export interface ExampleState {
  // Define the state properties for the `example` feature of the Redux store
}

export interface RootState {
  // Define the root state properties for the Redux store
}

export type AppAction = 
  // Define the action types for the application